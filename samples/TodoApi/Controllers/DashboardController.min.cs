namespace TodoApi.Controllers;[API,RT("api")]public class DashboardController(TodoDb db):Ctl{[HG("lists/{listId:int}/summary")]public async Tr Summary(int listId,CT ct=default){if(!await db.Lists.w(l=>l.Id==listId).any(ct))return nf();var tasks=await db.Tasks.nt().w(t=>t.ListId==listId).lst(ct);var now=DateTime.UtcNow;bool IsOpen(TodoTask t)=>t.Status!=TaskState.Done&&t.Status!=TaskState.Archived;var done=tasks.Count(t=>t.Status==TaskState.Done||t.Status==TaskState.Archived);return Ok(new ListSummary(listId,tasks.Count,tasks.GroupBy(t=>t.Status).ToDictionary(g=>g.Key.ToString(),g=>g.Count()),tasks.Count(t=>t.DueAt is{}d&&d<now&&IsOpen(t)),tasks.Count(t=>t.DueAt is{}d&&d>=now&&d<=now.AddDays(7)&&IsOpen(t)),tasks.Count==0?0:Math.Round(100.0*done/tasks.Count,1)));}}